# Operadores

## Operadores aritméticos:

    +, -, _, /, %, \*\*

## Incremento e decremento:

    ++, --

    let contador = 10;
    console.log(contador++); => 10
    console.log(contador); =>11
    console.log(++contador); =>12

    valor++ → usa e depois incrementa
    ++valor → incrementa e depois usa

## Operadores de atribuição:

    =, +=, -=, _=, /=, %=

## Operadores de comparação:

    ==, ===, !=, !==, >, <, >=, <=

## Operadores lógicos:

    &&, ||, !

## Avaliação de expressões

    Diferença entre == e ===

    Truthy e falsy

## Operador ternário:

    condição ? valorSeVerdadeiro : valorSeFalso
    O ternário é uma forma compacta de fazer uma decisão simples.

    Estrutura:
    condicao ? valorSeVerdadeiro : valorSeFalso

    Exemplo:
    let idade = 20;
    let resultado = idade >= 18 ? "Maior de idade" : "Menor de idade";
    console.log(resultado);

    Resultado:
    Maior de idade

Precedência dos operadores

Uso de parênteses para deixar expressões explícitas
