sort() — ideia central

O sort() reorganiza os elementos de um array.

Strings
const nomes = ["Carlos", "Ana", "Bruno", "Daniel"];

nomes.sort();

console.log(nomes);

Resultado:

["Ana", "Bruno", "Carlos", "Daniel"]

Por padrão, strings são ordenadas lexicograficamente (ordem semelhante à de um dicionário).
